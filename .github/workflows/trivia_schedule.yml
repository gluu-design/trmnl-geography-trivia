name: Generate Daily Geo Loop Data

on:
  schedule:
    # 6:00 AM EDT (Summer) / 5:00 AM EST (Winter)
    - cron: '0 10 * * *'
    
    # 6:00 PM EDT (Summer) / 5:00 PM EST (Winter)
    - cron: '0 22 * * *'
    
    # 7:00 PM EDT (Summer) / 6:00 PM EST (Winter) 
    # (Added to ensure the answer reveals during winter Daylight Saving Time)
    - cron: '0 23 * * *'
    
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-genai

      - name: Run Trivia Generator
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python trmnl_trivia.py

      - name: Commit Updated Data & History
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json history.json || true
          git diff --quiet && git diff --staged --quiet || (git commit -m "auto: update Geo Loop trivia feed" && git push)
